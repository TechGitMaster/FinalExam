import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.final_exam',
  appName: 'final_exam',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
